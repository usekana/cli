// const ROOT = "https://api.usekana.com/cli";
const ROOT = "http://localhost:3380/cli";

export const route = {
  validate: ROOT + "/validate",
  pull: ROOT + "/pull",
  publish: ROOT + "/publish",
};
